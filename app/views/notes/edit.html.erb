<h1>Editing note</h1>

<% @note.subnotes.each do |subnote| %>
	<%= best_in_place subnote, :content %><br>

<% end %>

<%= form_for @note.subnotes.build do |form| %>
	<%= form.text_area :content %><br>
	<%= form.hidden_field :note_id %><br>
	<%= form.submit "Add Subnote"%>
<% end %>

<%= link_to 'Show', @note %> |
<%= link_to 'Back', notes_path %>
